apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: kafka-allow-only-sellio
  namespace: sellio-data
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: kafka
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              namePrefix: sellio
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: sellio-data
      ports:
        - protocol: TCP
          port: 9092


