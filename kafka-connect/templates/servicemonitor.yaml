{{- if .Values.monitoring.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kafka-connect
  namespace: sellio-data
spec:
  namespaceSelector:
    matchNames: ["sellio-data"]
  selector:
    matchLabels:
      app.kubernetes.io/name: kafka-connect
  endpoints:
    - port: http
      interval: 30s
{{- end }}


