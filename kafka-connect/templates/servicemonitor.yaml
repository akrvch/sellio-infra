{{- if .Values.monitoring.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kafka-connect
  namespace: sellio-infra
spec:
  namespaceSelector:
    matchNames: ["sellio-infra"]
  selector:
    matchLabels:
      app.kubernetes.io/name: kafka-connect
  endpoints:
    - port: http
      interval: 30s
{{- end }}


