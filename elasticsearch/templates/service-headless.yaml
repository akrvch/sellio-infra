apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-headless
  namespace: sellio-infra
  labels:
    app.kubernetes.io/name: elasticsearch
spec:
  clusterIP: None
  ports:
    - name: transport
      port: 9300
      targetPort: 9300
  selector:
    app.kubernetes.io/name: elasticsearch


