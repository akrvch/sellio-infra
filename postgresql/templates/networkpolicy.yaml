apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: postgresql-allow-only-sellio
  namespace: sellio-data
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: postgresql
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              namePrefix: sellio
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: sellio-data
      ports:
        - protocol: TCP
          port: 5432


