apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: redis-allow-only-sellio
  namespace: sellio-infra
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: redis
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              namePrefix: sellio
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: sellio-infra
      ports:
        - protocol: TCP
          port: 6379


